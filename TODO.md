- [ ] Remove QR code generation from mainApp/services/certificate_service.py for both PDF certificate generation.
- [ ] Remove any QR code related layout/table cells in the generated PDF.
- [ ] Remove unused qrcode import if no longer required.
- [ ] Keep existing certificate ID and issued date on certificate.
- [ ] Run makemigrations --check --dry-run to ensure no DB changes.
- [ ] Run python manage.py test and a basic system check.
- [ ] (Optional) If any old certificates already stored QR images inside PDFs, regenerate them via regenerate-certificate endpoint.
