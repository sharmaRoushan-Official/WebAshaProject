# Git Auth Fix & Cleanup TODO

- [ ] Create/update .gitignore for __pycache__, db.sqlite3, venv/
- [ ] git add .gitignore && git commit -m "Add .gitignore"
- [ ] git push origin main (enter GitHub username & PAT)
- [ ] Test Django server: python manage.py runserver
- [ ] Optional: migrate if needed: python manage.py makemigrations && migrate

Updated: git status checked, credential helper set.
